<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.submission3jetpack">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Submission1Jetpack">
        <activity android:name="com.example.submission3jetpack.ui.splashscreen.SplashScreenActivity"
            android:theme="@style/Theme.Submission1Jetpack.NoActionBar">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.submission3jetpack.ui.detail.DetailActivity"
            android:label="@string/title_activity_detail" />
        <activity android:name="com.example.submission3jetpack.ui.home.HomeActivity"/>
        <activity android:name=".ui.home.FavoriteActivity"/>
    </application>

</manifest>