<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.submission2made">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.submission2made.app.application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Submission1Jetpack">
        <activity android:name="com.example.submission2made.app.splashscreen.SplashScreenActivity"
            android:theme="@style/Theme.Submission1Jetpack.NoActionBar">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.submission2made.app.detail.DetailActivity"
            android:label="@string/title_activity_detail" />
        <activity android:name="com.example.submission2made.app.home.HomeActivity"/>
    </application>

</manifest>